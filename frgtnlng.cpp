#include <bits/stdc++.h>
#define fastio               \
    ios::sync_with_stdio(0); \
    cin.tie(0);              \
    cout.tie(0);

using namespace std;

int main()
{
    fastio;
    short t, n, k, m;
    cin >> t;
    while (t--)
    {
        cin >> n >> k;
        string s1, s2;
        for (int i = 0; i < n; i++)
        {
            cin >> s1[i];
        }
        for (int j = 0; j < k; j++)
        {
            cin >> m;
            for (int k = 0; k < m; k++)
            {
                cin >> s1[k];
            }
        }
    }

    return 0;
}