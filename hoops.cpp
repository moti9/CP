#include <bits/stdc++.h>
#define fastio               \
    ios::sync_with_stdio(0); \
    cin.tie(0);              \
    cout.tie(0);

using namespace std;

int main()
{
    fastio;
    int t;
    cin >> t;
    while (t--)
    {
        long int n;
        cin >> n;
        int count = 0;
        int a = 1, b = n;
        for (int i = 0; i < n; i++)
        {
            if (a <= n && b <= n)
            {
                //
            }
        }
    }

    return 0;
}
// incomplete
