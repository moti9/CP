#include<bits/stdc++.h>
#define fastio ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);

using namespace std;


int main()
{
    fastio;
    int t;
    cin>>t;
    while (t--)
    {
        short m,x,y;
        cin>>m>>x>>y;
        int a[m],d=x*y,sum=0;
        for (int i = 0; i < m; i++)
        {
          cin>>a[i];
        }
        sort(a,a+m);
        
    }
    
    

    return 0;
}