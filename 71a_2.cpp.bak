#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    cin >> n;
    while (n--)
    {
        string c;
        getline(cin >> ws, c);
        if (c.length() <= 10)
            cout << c << "\n";
        else
        {
            cout << c[0] << (c.length() - 2) << c[c.length() - 1] << endl;
        }
    }
    return 0;
}